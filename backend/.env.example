DATABASE_URL="postgresql://postgres:admin123@localhost:5433/tanit"
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
PORT=3001
FASTAPI_URL="http://localhost:8000"
FRONTEND_ORIGIN="http://localhost:5174"
UPLOAD_DIR="./uploads"

# Optional : emails candidature / décision recruteur (ex. Gmail : mot de passe d’application, sans espaces dans SMTP_PASS)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="votre-compte@gmail.com"
# SMTP_PASS=""
# SMTP_FROM="" 
# SMTP_DEBUG="1"   # logs SMTP dans la console backend (dépannage)
